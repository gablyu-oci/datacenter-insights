# API base URL — leave empty for relative URLs (works with reverse proxy)
# For local dev against a separate backend:
# VITE_API_BASE_URL=http://localhost:8000
VITE_API_BASE_URL=

# Google Maps API key (get your own; do NOT commit real keys)
VITE_GOOGLE_MAPS_API_KEY=

# Pinned dependencies for strategic-insights-backend
# Use this file as a fallback when uv is not available:
#   pip install -r requirements.txt

fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlmodel==0.0.24
asyncpg==0.30.0
alembic==1.15.2
polars==1.30.0
openpyxl==3.1.5
rapidfuzz==3.12.2
httpx==0.28.1
stamina==24.3.0
apscheduler==3.11.0
python-dotenv==1.1.0
pydantic-settings==2.8.1
openai==1.78.0
edgartools==3.5.1

# PDF generator-permit parser (Phase 1.5 step 2)
# Optional system binaries (install separately):
#   sudo apt-get install -y tesseract-ocr poppler-utils
pdfplumber==0.11.9
pytesseract==0.3.13
pdf2image==1.17.0
anthropic==0.97.0

# HTML scraping for state-permit adapters (TCEQ NSR pending permits)
beautifulsoup4==4.14.3

# AWS SDK — daily Aterio inventory pull from S3 access point.
# Auth via the 'aterio' profile in ~/.aws/credentials.
boto3>=1.34,<2.0

# OCI SDK — daily Aterio snapshot mirror to Object Storage.
# Auth via ~/.oci/config (profile selected by OCI_CONFIG_PROFILE).
oci>=2.126,<3.0
